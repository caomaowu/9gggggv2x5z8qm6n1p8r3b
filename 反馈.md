览_代理的定位就是透明透传一不做任何语义转换，客户真接对接OKX原生API。这个设计意图本身就是让熟悉OKX的开发者直接用OKX参数名和语义。
客户需要改的只有一点：把「获取某时间之前数据」的before改成after：#错误（客户的直觉）
curl "..-/api/v5/market/candles?instId=BTC-USDT-SWAP&bar=1H&limit=3&before=1777612800000"#正确（OKX原生语义）
curl2e/api/v5/narket/candles?instId=BTC-USDT-SWAP&bar=1H&limit=3&after=1777612800000"如果客户觉得okx原生命名反直觉，还可以引导他们用api/v1/ohlcv，那里用的是更直观的start_time/end_time（Is08601格式），参数名和语义都是"正常人思维”。
总结：不修代理，文档里加一句说明就行。要我更新bug文档标记为"非代理问题，系OKXAPI设计行为