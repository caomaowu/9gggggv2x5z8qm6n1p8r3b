WARNING - [brale-mechanics] No mechanics_compressed; returning default.
这个警告的具体含义
在 brale_mechanics_agent（力学分析代理）中，代码预期会收到一个名为 mechanics_compressed 的字段（通常是经过特征工程压缩后的力学指标数据，如资金费率、持仓量变化、多空比等）。但实际运行时，这个字段不存在或为空，因此代理无法进行正常分析，只能返回一组默认值（得分为0、置信度为0、杠杆/拥挤度/风险为 unknown）。

这严重吗？
不严重，属于数据预处理环节的缺失，而不是底层数据获取失败。

从日志看：

6种衍生品原始数据都成功获取了（oi, funding_history, long_short_history 等）

但之后某个负责将原始数据加工成 mechanics_compressed 的步骤没有执行成功或没有生成该字段

最终的综合分析仍然完成了（使用了另外两个 agent 的结果），只是力学部分的贡献为0。

可能的原因
数据格式不匹配：获取到的衍生品数据结构和 mechanics_agent 期望的输入结构不一致，导致特征提取函数跳过了压缩步骤。

缺失特征工程函数：代码中可能有一个 build_mechanics_compressed() 或类似的函数没有被调用。

时间范围问题：虽然获取了6个数据集，但可能这些数据的时间范围与当前分析的时间窗口（4h K线）没有对齐，导致压缩函数返回空。

代码逻辑分支：在 analyze.py 或 mechanics_agent 中，当某条件不满足时（比如历史资金费率点数不足），直接跳过了压缩字段的生成。

建议的排查方向
检查 backend/app/agents/brale_mechanics_agent.py 或 backend/app/api/v1/endpoints/analyze.py 中：

调用 mechanics_agent 之前，是否有类似 mechanics_compressed = build_compressed_features(derivative_data) 的代码

该函数的输出是否被正确赋值到请求 payload 的 mechanics_compressed 字段

如果你希望彻底消除这个警告，可以进一步定位那段缺失的压缩逻辑。但从系统运行角度看，当前状态已经是可用且稳定的了。